import { AdminPageHeader } from "@/features/admin/components/admin-page-header";

export default function AdminDashboardPage() {
  return (
    <AdminPageHeader
      eyebrow="Admin Workspace"
      title="Dashboard"
      description="The responsive Admin workspace shell is active. Database-backed dashboard content will be added in the next implementation unit."
    />
  );
}
